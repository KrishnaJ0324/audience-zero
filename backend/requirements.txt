# Runtime deps — read by both the local venv and the Databricks Apps build.
# fastapi/uvicorn are deliberately unpinned: the Apps runtime preinstalls them and
# a conflicting pin resolves slowly or crash-loops the app. Test-only deps live in
# requirements-dev.txt so they never reach the deployed image.
fastapi
uvicorn[standard]
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2
aiosqlite==0.20.0
python-multipart==0.0.20
httpx==0.28.1
openai==1.59.6
sse-starlette==2.2.1
reportlab==4.2.5
